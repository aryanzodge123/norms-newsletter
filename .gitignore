.env
.venv/
__pycache__/
*.pyc
.DS_Store
node_modules/
dist/
site/dist/
# wrangler's local cache (account id, build state). Machine-specific, rewritten
# on every deploy, and never wanted in the repo.
.wrangler/
